{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run dev --parallel",
    "start": "lerna run start --parallel",
    "test": "lerna run test --parallel",
    "install": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
